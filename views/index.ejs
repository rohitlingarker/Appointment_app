<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <!-- views/appointments/index.ejs -->
<header>
  <h1 class="text-4xl font-bold mb-4 m-4">Appointments</h1>
  <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">New</button>
</header>
<div class="flex justify-center w-full ">
  <table class="table-fixed w-5/6">
    <thead>
      <tr>
        <th class="shadow border-2 bg-yellow-300 px-4 py-2">Event Name</th>
        <th class="shadow border-2 bg-yellow-300 px-4 py-2">Date</th>
        <th class="shadow border-2 bg-yellow-300 px-4 py-2">Start Time</th>
        <th class="shadow border-2 bg-yellow-300 px-4 py-2">End Time</th>
        <th class="shadow border-2 bg-yellow-300 px-4 py-2">Description</th>
        <th class="shadow border-2 bg-yellow-300 px-4 py-2">Actions</th>
      </tr>
    </thead>
    <tbody>
      <% for(let i=0;i<appointments.length;i++){ %>
        <tr>
          <td class="border text-center px-4 py-2"><%= appointments[i].eventName %></td>
          <td class="border text-center px-4 py-2"><%= appointments[i].date%></td>
          <td class="border text-center px-4 py-2"><%= appointments[i].startTime %></td>
          <td class="border text-center px-4 py-2"><%= appointments[i].endTime %></td>
          <td class="border text-center px-4 py-2"><%= appointments[i].description %></td>
          <td class="border text-center px-4 py-2 flex justify-evenly">
            <form method="POST" action="/appointments/<%= appointments[i]._id %>?_method=DELETE">
              <button type="submit" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">
                Delete
              </button>
            </form>
            <a href="/appointments/<%= appointments[i]._id %>/edit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
              Edit
            </a>
          </td>
        </tr>
        <% } %>
      </tbody>
    </table>
  </div>
    
    

</body>
</html>